package tech.agrowerk.infrastructure.model.barter.enums;

public enum ContractStatus {
    DRAFT,
    AWAITING_OFFEROR_SIGNATURE,
    AWAITING_ACCEPTOR_SIGNATURE,
    ACTIVE,
    COMPLETED,
    CANCELLED,
    EXPIRED
}
